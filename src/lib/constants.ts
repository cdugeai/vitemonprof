// MAP

export const MAP_DEFAULT_LAT = 46.2;
export const MAP_DEFAULT_LNG = 2.3;
export const MAP_DEFAULT_ZOOM = 4;

// MAP — school clustering
// Above this zoom level, clusters break apart into individual schools.
export const MAP_CLUSTER_MAX_ZOOM = 12;
// Radius, in pixels, within which points get merged into a single cluster.
export const MAP_CLUSTER_RADIUS = 50;
